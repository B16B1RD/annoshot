namespace Annoshot.Core;

/// <summary>
/// UI 非依存で参照できる製品情報。
/// </summary>
public static class AppInfo
{
    /// <summary>製品名。ウィンドウタイトル等の表示名として使う。</summary>
    public const string Name = "annoshot";
}
