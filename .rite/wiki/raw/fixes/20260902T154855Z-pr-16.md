---
type: fixes
source_ref: "pr-16"
captured_at: "2026-09-02T15:48:55+00:00"
pr_number: 16
title: "PR #16 fix results"
ingested: false
---

## Fix Results

- **PR**: #16
- **Type**: fix
- **Fixed at**: 2026-09-03 (JST)

### Fix Patterns
- 環境制約で Issue の MUST を計測できないとき、コードではなく文書側で結論の確定度（暫定）と未達 AC を明示することで指摘を解消する。実機の表示設定変更のような不可逆・外向きの操作は自律実行せずユーザーに委ねる。
- Avalonia + OnExplicitShutdown で fire-and-forget な async 起点を持つ場合、初期化の同期失敗で即 Shutdown するとメインループ開始前になり「Dispatcher shut down」で落ちる。起動を Dispatcher.UIThread.Post で遅延させると exit code が正しく返る（実機で exit 1 を確認）。
- CLI の既定出力先は CWD 相対にせず AppContext.BaseDirectory 配下（bin/）に閉じると、文書化した実行手順がリポジトリを汚さない。
- 無言クランプする Crop と生 BitBlt を突き合わせる前に、矩形を入口で 1 度だけ交差させて全消費者に同じ矩形を渡す。
- P/Invoke 失敗を既定値に潰す fallback は計測目的のコードでは throw に置き換える。

### Statistics
- Total findings: 10
- Fixed: 10
- Replied: 0

